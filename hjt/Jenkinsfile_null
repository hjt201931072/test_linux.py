pipeline {
    agent any
    parameters {
        file(name: 'dataset', description: 'Single-cell sequencing dataset')
    }
    stages {
        stage('Preprocessing') {
            steps {
                sh "python3 normal.py preprocess ${params.dataset}"
            }
        }
        stage('Clustering') {
            steps {
                sh "python3 normal.py cluster"
            }
        }
        stage('Visualization') {
            steps {
                sh "python3 normal.py visualize"
            }
        }
    }
}
